spring:
  profiles:
    active: test
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:h2:tcp://localhost/~/bitlibrary
    username: aa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
